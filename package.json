{
  "name": "fm-promise",
  "version": "1.5.0",
  "description": "This library returns a promise when provided an array of URLs. Calls all the URLs and compiles response as one and returns as a promise",
  "main": "./lib/src/index.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha --require babel-core/register",
    "start": "node bin/dev",
    "clean": "rm -rf lib",
    "build": "npm run clean && mkdir lib && babel . -s -d lib --ignore node_modules,test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.7.1",
    "request": "^2.88.0",
    "request-promise": "^4.2.5"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-polyfill": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "babel-register": "^6.26.0",
    "chai": "^4.2.0",
    "mocha": "^10.2.0",
    "sinon": "^7.5.0"
  }
}
